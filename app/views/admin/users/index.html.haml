%h1= @page_title

%p.text-right
  = link_to 'Invite User(s)', invite_admin_users_path, class: 'btn btn-primary'
  = link_to 'View Roles', effective_roles.admin_roles_path, class: 'btn btn-primary'

= render_datatable(@datatable)
