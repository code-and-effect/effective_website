%p You belong to the following client groups: #{'none' if user.mates.length == 0}

%ul
  - user.mates.each do |mate|
    %li #{link_to(mate.client, '#')} as a <strong>#{mate.roles.first}</strong>.

= render 'mates/roles'
