%h1= @page_title

%p Accept your invitation to create a user account on this website.

= simple_form_for(resource, as: resource_name, url: invitation_path(resource_name), html: { method: :put }) do |f|
  = f.input :invitation_token, as: :hidden

  .row
    .col-sm-6
      = render 'users/sign_up_fields', f: f
    .col-sm-6
      = effective_roles_summary(f.object)

  = f.button :submit, 'Accept Invitation', class: 'btn btn-primary', data: { disable_with: 'Accepting...' }
