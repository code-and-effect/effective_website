!!!
%html{lang: 'en'}
  %head
    %meta{charset: 'utf-8'}
    %meta{content: 'IE=edge', 'http-equiv': 'X-UA-Compatible'}
    %meta{content: 'width=device-width, initial-scale=1', name: 'viewport'}
    %meta{content: 'index, follow', name: 'robots'}
    = csrf_meta_tags

    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'

    = effective_pages_header_tags

  %body{class: effective_pages_body_classes}
    #header-wrap
      = render partial: 'layouts/navbar'

      .container
        - if content_for?(:breadcrumb)
          = yield :breadcrumb
        - else
          = render_breadcrumbs(:main, @page_title)

    #content-wrap
      = render partial: 'layouts/alert'

      .container
        - if content_for?(:sidebar)
          .row
            .col-sm-3#sidebar= yield :sidebar
            .col-sm-9#main= yield
        - else
          .row
            .col-sm-12#main= yield

    #footer-wrap
      = render partial: 'layouts/footer'

    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload', 'data-turbolinks-eval': 'false'
    = effective_regions_include_tags
