= tabs do
  = tab 'Demographics' do
    = render 'form_client', client: client, namespace: nil

  - if client.persisted?
    = tab 'Users' do
      = render 'mates/roles'
      = render_datatable(MatesDatatable.new(client_id: client.id), inline: true)
